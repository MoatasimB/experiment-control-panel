services:
  control-plane:
    image: node:22-alpine
    working_dir: /app
    command: sh -c "npm install && npm run build && npm run dev"
    ports:
      - "4173:4173"
    volumes:
      - ../..:/app
