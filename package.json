{
  "name": "experiment-reliability-control-plane",
  "version": "1.0.0",
  "private": true,
  "description": "Full-stack experiment, rollout, and reliability control plane demo.",
  "type": "module",
  "scripts": {
    "dev": "node backend/src/server.js",
    "dev:api": "node backend/src/server.js",
    "dev:web": "vite frontend --host 127.0.0.1",
    "build": "vite build frontend",
    "preview": "npm run build && node backend/src/server.js",
    "start": "node backend/src/server.js",
    "test": "node --test tests/*.test.js",
    "seed": "node scripts/seed.js",
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose down",
    "db:reset": "docker compose down -v && docker compose up -d postgres",
    "db:psql": "docker compose exec postgres psql -U rcp_user -d rcp_dev"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.0",
    "typescript": "^5.6.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=20"
  }
}
